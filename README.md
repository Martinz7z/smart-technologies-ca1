# Smart Technologies CA1

Image classification project using selected classes from the CIFAR-10 and CIFAR-100 datasets.

The objective is to preprocess and combine the datasets and train a convolutional neural network to classify images into 24 classes.